import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    background: {
      default: '#eeeee4', // Aquí puedes establecer el color de fondo deseado
    },
  },
});

export default theme;